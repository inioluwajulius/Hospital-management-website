{
  "name": "hospital-management-system",
  "version": "1.0.0",
  "description": "Hospital Management System - Full Stack Application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd hospital-backend && npm start\" \"cd hospital-frontend && npm run dev\"",
    "build": "npm run build:frontend",
    "build:frontend": "cd hospital-frontend && npm run build",
    "build:backend": "cd hospital-backend && npm install",
    "install-all": "npm install && cd hospital-backend && npm install && cd ../hospital-frontend && npm install"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.3.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "18.x"
  }
}
